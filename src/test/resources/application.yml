spring:
  devtools:
    add-properties: false
  datasource:
    driverClassName: org.h2.Driver
    jdbc-url: jdbc:h2://mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa

logging:
  level:
    web: 'DEBUG'